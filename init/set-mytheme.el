; —’…´≈‰÷√
(provide 'set-mytheme)

(require 'color-theme)
;(eval-after-load "color-theme"
;  '(progn
;     (color-theme-initialize)
;     (load-file blackboard-theme)
;     (color-theme-blackboard)))
